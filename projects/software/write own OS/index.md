---
title: "GABROS"
description: "Custom operating system with graph-based filesystem, native versioning, and a custom orchestration language."
status: "Idea"
category: "Software"
tags: ["Operating System", "Filesystem", "Programming Language", "Systems"]
published: false
---

- every folder is also a file that can have content
- files are nodes and stored and indexed as a flat list
- file structures are not tree structures but rather graph structures, files/nodes can be referenced in multiple "localtions"
- each file has a native metadata key value storage, seprated from content

- GABROS
- native versioning system

custom programming language:

x ∈ string

orchestration/ controller functions
handler(x ∈ string)
{
validate(x)
branch()
}
